# Allow the default `guest` user to connect from other Docker services (not only 127.0.0.1),
# so Node-RED and business services can use `amqp://guest:guest@rabbitmq:5672` in Docker Compose.
loopback_users = none
